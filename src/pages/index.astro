---
import "@fontsource/open-sans"
import Layout from '../layouts/Layout.astro';
import Navbar from '@components/common/Navbar.astro';
import LandingTextArea from '@components/LandingTextArea.astro';
import SkillArea from "@components/skills/SkillArea";
import ProjectArea from "@components/projects/ProjectArea.astro";
import Footer from "@components/common/Footer.astro";
---

<Layout title="Parthiv Menon">
	<main>
		<Navbar />
		<div class="flex landing-container">
			<div class="image-wrapper flex">
				<img src="/assets/images/whoami-black-blob.png" class="whoami-blob" />
			</div>
			<div class="landing-text">
				<div class="about">
					<h4 style="margin-bottom: 0; font-size: 1.2rem;">I am Parthiv Menon.</h4>
					<h4 style="margin-top: 0">
						Passionate about anything that involves creativity,
						enthusiastic about working on any idea that can get me thinking, and love
						to play basketball
					</h4>
				</div>
				<LandingTextArea />
				<div class="flex">
					<div class="dot yellow"></div>
					<div class="dot violet"></div>
					<div class="dot pink"></div>
					<div class="dot blue"></div>
				</div>
			</div>
		</div>
		<div class="skills-container flex flex-col">
			<div class="about about-mobile">
				<h4 style="margin-bottom: 0; font-size: 1.2rem;">I am Parthiv Menon.</h4>
				<h4 style="margin-top: 0">
					Passionate about anything that involves creativity,
					enthusiastic about working on any idea that can get me thinking, and love
					to play basketball
				</h4>
			</div>
			<SkillArea client:visible />
		</div>
		<div class="projects-container" id="projects">
			<ProjectArea />
		</div>
	</main>
	<Footer />
</Layout>

<style>
	/* landing page styling */
	.landing-container {
		padding: 2rem;
		padding-top: 4rem;
		max-width: 100vw;
		height: calc(100vh - 60px);
		justify-content: center;
		align-items: center;
	}

	.image-wrapper {
		height: 100%;
		width: 50%;
		justify-content: center;
		align-items: center;
	}

	.landing-text {
		width: 50%;
		height: 80%;
		padding: 30px;
	}

	.whoami-blob {
		height: 500px;
		width: 500px;
	}

	.about {
		width: 70%;
		border-left: 5px solid rgb(43, 42, 42);
		padding-left: 25px;
	}

	.about h4:first-child {
		color: var(--primary-colour);
	}

	.about h4:nth-child(2) {
		color: var(--secondary-dark);
		font-style: italic;
	}

	.dot {
		height: 15px;
		width: 15px;
		margin-right: 10px;
		border-radius: 5px;
	}

	.yellow {
		background-color: #5cb904;
	}

	.violet {
		background-color: #6005c1;
	}

	.pink {
		background-color: #c50170;
	}

	.blue {
		background-color: var(--primary-colour);
	}

	.about-mobile {
		display: none;
	}

	/* skills styling */
	.skills-container {
		min-height: 100vh;
		justify-content: center;
		align-items: center;
	}

	/* projects styling */
	.projects-container {
		min-height: 100vh;
		padding: 2rem 0;
	}

	@media only screen and (max-width: 820px) {

		/* landing page mobile styling */
		.landing-container {
			flex-direction: column;
			padding-top: 2rem;
		}

		.whoami-blob {
			height: 300px;
			width: 300px;
		}

		.image-wrapper,
		.landing-text {
			width: 90%;
		}

		.image-wrapper {
			height: 50%;
			align-items: start;
		}

		.about {
			display: none;
		}

		.dot {
			display: none;
		}

		.landing-text {
			rotate: 90deg;
			width: fit-content;
			height: fit-content;
		}

		.about-mobile {
			display: block;
			width: 80%;
			margin: 50px auto;
		}
	}
</style>