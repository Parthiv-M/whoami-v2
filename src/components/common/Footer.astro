<footer style="z-index: 100">
    <div class="flex upper-half">
        <div class="flex">
            <a href="https://github.com/parthiv-m" target="_blank"><img alt="Github" src="/assets/icons/connect/github.svg" height="20px" width="20px" /></a>
            <a href="https://instagram.com/_.parthiv_" target="_blank"><img alt="Instagram" src="/assets/icons/connect/instagram.svg" height="20px" width="20px" /></a> 
            <a href="https://linkedin.com/in/parthivmenon" target="_blank"><img alt="Linkedin" src="/assets/icons/connect/linkedin.svg" height="20px" width="20px" /></a>   
        </div>
        <div class="scroll-to-top flex">
            Back to Top<span><img alt="Right arrow" src="/assets/icons/arrow-right.svg" height="15px" width="12px" /></span>
        </div>
    </div>
    <div class="flex lower-half">
        <p>2023 • Parthiv Menon • <span class="made-with">Built with <a target="_blank" href="https://astro.build/">Astro</a></span></p>
    </div>
</footer>

<style>
    .upper-half {
        justify-content: space-between;
        align-items: center;
        padding: 2rem;
    }

    .upper-half div {
        gap: 10px;
    }

    .scroll-to-top {
        background-color: var(--primary-colour);
        padding: 7px 10px;
        border-radius: 1.5rem;
        align-items: center;
        gap: 7px;
        cursor: pointer;
    }

    .flex {
        display: flex;
    }

    .scroll-to-top span {
        rotate: -90deg;
    }

    .scroll-to-top:hover span {
        transform: translateX(3px);
        transition: all 500ms;
    }

    .lower-half {
        background-color: var(--tertiary-dark);
        padding: 1rem 2rem;
    }

    .made-with {
        color: var(--secondary-dark);
    }

    .made-with a {
        text-decoration: underline;
    }

    @media only screen and (max-width: 820px) {
        .lower-half {
            justify-content: center;
            padding: 2rem;
        }
    }
</style>

<script>
    // scroll to top of the page
    const scrollToTop = () => {
        document.body.scrollTop = 0; // For Safari
        document.documentElement.scrollTop = 0; // For Chrome, Firefox, IE and Opera
    }
    const scrollButton = document.querySelector<HTMLElement>(".scroll-to-top")!;
    scrollButton.addEventListener("click", () => {
        scrollToTop();
    });
</script>